<h1><%= @dog.name %></h1>
<li><%= @dog.age %></li>
<li><%= @dog.breed %></li>
<h2>Owners</h2>
<% @dog.employees.each do |e| %>
    <li><%= link_to "#{e.first_name} #{e.last_name}", employees_path(e) %></li>
<% end %>
